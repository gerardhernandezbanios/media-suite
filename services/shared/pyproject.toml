[tool.poetry]
name = "shared"
version = "0.1.0"
description = "Shared kernel for media-suite"
authors = ["Gerard"]
packages = [{ include = "shared", from = "src" }]

[tool.poetry.dependencies]
python = "^3.12"
pydantic = "^2.0"
httpx = "^0.27"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
